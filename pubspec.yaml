name: fitnessworkout
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  google_fonts: ^7.0.2
  fl_chart: ^1.1.1

  firebase_core: ^3.8.1
  firebase_auth: ^5.3.3
  cloud_firestore: ^5.5.2
  firebase_storage: ^12.4.10
  firebase_app_check: ^0.3.2+10 # ✅ ADDED

  intl: ^0.20.2
  permission_handler: ^12.0.1
  image_picker: ^1.2.1
  camera: ^0.11.0+2
  video_player: ^2.10.1
  chewie: ^1.13.0
  google_mlkit_face_detection: ^0.13.1
  http: ^1.6.0
  google_ml_kit: ^0.20.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/video/
